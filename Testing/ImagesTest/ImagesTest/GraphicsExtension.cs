using System;
using System.Collections.Generic;
using System.Drawing;
using System.Linq;
using System.Text;
using System.Threading.Tasks;

namespace ImagesTest {
    public static class GraphicsExtension {

        //private void draw_circle(this Graphics graphicName, Pen pen, float x, float y, float radius) {
        //    graphicName = this.CreateGraphics();
        //    pen = new Pen(Color.Blue, 3);
        //    graphicName.DrawEllipse(pen, x - radius, y - radius, radius + radius, radius + radius);
        //}
    }
}
